#include <iostream>
using namespace std;
int main()
{
    //Operating System(it manages the hardware) executes all programs
    cout << "Hello World!" << endl<<"This is a new line"<<endl;//endl->create a new line
    //Purpose of compiler->translate the source code to understable source code to the Operating System
    //cout->is a object of a class ostream
    //ostream->is a template/class that contains some functions.These functions can control the flow of bits to some destination.can take some bits to memory ,can deliver bits to some destination.
    //ostream->responsible/having some functions which can take some bits from the memory and deliver some bits to destination.
    //cout->fixed destination
    // <<->insertion operator. inserts bits from memory top output object
    return 0;
}